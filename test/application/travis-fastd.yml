---
- name         : Install and configure fastd
  hosts        : "{{ kitchen_hosts }}"
  user         : travis
  become       : true
  gather_facts : true
  connection   : "{{ kitchen_connection | default('local') }}"
  vars_files   :
                 - "fastd_pkg_vars.yml"
  roles        :
                 - "ansible-role-fastd-travis"
