---
- name         : Install and configure fastd (defaults)
  hosts        : "{{ kitchen_hosts }}","all"
  gather_facts : true
  connection   : "{{ kitchen_connection | default('local') }}"
  vars_files   :
                 - "fastd_pkg_vars.yml"
  roles        :
                 - "ansible-role-fastd-travis"
